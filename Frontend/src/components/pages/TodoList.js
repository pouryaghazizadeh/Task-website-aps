import React from "react";
import { Box, Typography } from "@mui/material";

const TodoList = () => {
  return (
    <Box py={20} textAlign="center">
      <Typography variant="h1">TodoList Page!</Typography>
    </Box>
  );
};

export default TodoList;